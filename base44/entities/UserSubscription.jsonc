{
  "name": "UserSubscription",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Email of the subscribed user"
    },
    "stripe_customer_id": {
      "type": "string",
      "description": "Stripe customer ID"
    },
    "stripe_subscription_id": {
      "type": "string",
      "description": "Stripe subscription ID"
    },
    "plan": {
      "type": "string",
      "enum": [
        "stories",
        "pro",
        "expert"
      ],
      "description": "stories = unlimited messages; pro = messages + market + card show; expert = all + trending"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "cancelled"
      ],
      "default": "inactive",
      "description": "Subscription status"
    },
    "current_period_end": {
      "type": "string",
      "description": "ISO date when subscription ends"
    }
  },
  "required": [
    "user_email"
  ]
}